ktor:
  deployment:
    port: 8080
  application:
    modules:
      - com.example.ApplicationKt.module

database:
  host: $DB_HOST
  port: $DB_PORT
  database: $DB_NAME
  user: $DB_USER
  password: $DB_PASSWORD
  poolSize: $DB_POOL_SIZE
  ssl: $DB_SSL